<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="Travel">
  
    <!-- 시퀀스 번호값 검색 -->
    <select id="mateNoSeq_Find" resultType="java.lang.Long">
      select mate_no_seq.nextval from dual <!-- 시퀀스 다음 값 가져오는 쿼리문 -->
    </select>
     
     
  </mapper>   